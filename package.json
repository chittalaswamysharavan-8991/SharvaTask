{
  "name": "sharvatask-mcp-vercel-v2",
  "version": "2.4.0",
  "private": true,
  "description": "TaskState MCP: persistent work-state infrastructure for AI assistants, with durable list/task history, proof, authoritative read-back, and ChatGPT UI.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/foundation-contract.test.mjs tests/routing-intent.test.mjs tests/widget-phase-f.test.mjs",
    "verify:descriptor": "node scripts/verify-descriptor.mjs",
    "verify:phase-f": "node scripts/verify-phase-f.mjs",
    "verify:foundation": "node scripts/verify-foundation.mjs",
    "verify": "npm run verify:foundation && npm run verify:descriptor && npm run verify:phase-f && npm run typecheck && npm run build && npm run test"
  },
  "dependencies": {
    "@vercel/blob": "latest",
    "mcp-handler": "latest",
    "next": "16.3.0",
    "react": "latest",
    "react-dom": "latest",
    "zod": "latest",
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest",
    "yaml": "^2.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}
